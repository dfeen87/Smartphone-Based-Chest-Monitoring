# RespiroSync Dashboard Server — Python dependencies
flask>=2.3
numpy>=1.21
scipy>=1.7
# matplotlib is optional (used only by validate_bidmc.py, not the server)
# wfdb is optional (needed only for live PhysioNet downloads)
